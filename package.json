{
  "name": "vite",
  "version": "0.0.0",
  "scripts": {
    "serve-lambda" : "SECRET=42 netlify-lambda serve src/lambda",
    "serve-vite" : "vite",
    "dev": "SECRET=42 netlify-lambda-serve src/lambda & vite",
    "build": "vite build & netlify-lambda build",
    "serve": "SECRET=42 netlify-lambda serve src/lambda & vite"
  },
  "devDependencies": {
    "vite": "^3.0.4"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.15"
  }
}

{
  "name": "vite",
  "version": "0.0.0",
  "scripts": {
    "serve-lambda" : "netlify-lambda serve src/lambda",
    "serve-vite" : "vite preview",
    "dev": "netlify-lambda-serve src/lambda & vite",
    "build": "vite build",
    "serve": "netlify-lambda serve src/lambda & vite preview"
  },
  "devDependencies": {
    "vite": "^3.0.4"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.15"
  }
}
